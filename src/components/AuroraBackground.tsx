import React from 'react';
//import './AuroraBackground.css'; // Or your global index.css

/**
 * A full-screen animated gradient background.
 * Relies on the CSS definitions in a global stylesheet (e.g., index.css or AuroraBackground.css).
 */
export const AuroraBackground: React.FC = () => {
  return <div className="aurora-background" />;
};